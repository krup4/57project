spring:
  application:
    name: printerApplicationTest
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: admin
    password: admin
  jpa:
    show-sql: true
config:
  secret: test